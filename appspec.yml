version: 0.0
os: linux

files:
  - source: /
    destination: /usr/share/nginx/html
    file_exists_behavior: OVERWRITE

hooks:
  AfterInstall:
    - location: scripts/validate.sh
      timeout: 60
      runas: root